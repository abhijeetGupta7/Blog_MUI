import { styled } from "@mui/material/styles";
import { AppCardTitle } from "../AppCardTitle";

export const HeroTitle = styled(AppCardTitle)({
  lineHeight: 1.1,
});

HeroTitle.displayName = "HeroTitle";
